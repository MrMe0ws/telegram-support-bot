services:
  support-bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/data
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M
